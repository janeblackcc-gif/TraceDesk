# 证据规划改动后的浏览器验收

2026-09-08，验收应用提交为 `8b65f813e93fdffb21ad6b56eb74044d338ed93b`。运行前检查本机新实例、独立空知识库及模型就绪，服务提供的 `web/app.js` 与工作区字节一致，SHA256 为 `344be968fd7267294a8f7677201a2006f3ae49d112f2db8bca5fc8e6b1a1b1ec`。

使用 Playwright 1.62.0 和本机 Microsoft Edge，无头模式、真实HTTP页面导航与原生浏览器请求，不注入模拟答案。15:13:22 至 15:14:08 UTC，46.605秒、退出码0，[原始功能报告](report.json)记录38项检查、17次截图和3次导出一致性验证。公开资料均为原创虚构测试内容。

- 空库、演示资料和v1/v2端口隔离通过。
- 真实模型回答7小时；替换同名文件并重新索引后回答13小时，不再输出旧值。
- 无联系电话资料时返回 `no_evidence`，正文为空且说明缺失事实。
- 两次回答和一次拒答的 `generation_assessment` 在HTTP响应、持久化追踪、页面和Markdown导出中相同。
- 原文引用高亮通过，测试文档删除后旧来源与旧范围失效。
- 桌面1440×1000与手机390×844视口没有横向溢出，未记录页面JavaScript错误或网络失败。

## 截图与导出

- [回答及生成记录](desktop-answer-record.png)
- [拒答及缺失事实](desktop-refusal-record.png)
- [手机实际滚动视口](mobile-viewport.png)
- [修正捕获后的手机长截图](mobile-corrected.png)
- [7小时答案导出](model_original_interval_answer.md)
- [拒答导出](model_no_answer_answer.md)
- [13小时答案导出](model_updated_interval_answer.md)

## 捕获与工具诊断

首轮长截图在页面已滚动时将sticky导航及屏外跳转链接捕获到错误位置。后续记录[实际视口几何](mobile-geometry.json)，滚动592像素和回到页顶时导航均位于视口y=0，未聚焦的跳转链接均在屏外。验收脚本截图前回到页顶并等待渲染后，手机截图正常；无需修改应用布局。公开手机图片来自补查的演示证据模式，生成记录的三端一致性仍以首轮真实模型检查为依据。

首轮功能检查通过后，关闭浏览器时出现 `Response.finished()` 留下的 `Target closed` 后台任务异常。所安装版本的该方法创建了关闭等待任务，响应完成时未回收；脚本改用已经等待完整响应体的 `response.json()`。补查于15:17:19 UTC完成，退出码0，无同类关闭异常。补查使用既有演示资料，验证截图与响应处理修改，没有重跑或替代首轮完整功能报告。

这些是受控功能检查，不能证明一般问题的语义正确率；手机尺寸模拟也不等于真机触控或其他浏览器引擎验收。原始日志与全部截图保留在本机独立测试输出目录。
